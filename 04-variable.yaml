- name: demo on variables
  hosts: localhost
  connection: local
  vars:
   course: "Ansible"
   trainer: "Siva Kumar"
   duration: "150hrs"
  tasks:
   - debug:
      msg: "Hi ,I am learing {{course}},trainer is {{trainer}}, duration is {{duration}}"
      