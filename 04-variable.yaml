- name: demo on variables play level
  hosts: presenthost
  connection: local
  vars:
    course: "Ansible"
    trainer: "Siva Kumar"
    duration: "150HRS"
  tasks:
  - name: text
    debug:
       msg: "Hi ,I am learing {{course}},trainer is {{trainer}}, duration is {{duration}}"
